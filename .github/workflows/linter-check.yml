name: linter-check

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - run: cd app
      - run: chmod +x gradlew

      - run: ./gradlew checkstyleMain
      - run: ./gradlew checkstyleTest
